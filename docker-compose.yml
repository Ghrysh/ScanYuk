services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: scanyuk-app
    container_name: scanyuk-app
    restart: unless-stopped
    working_dir: /var/www
    dns:
      - 8.8.8.8
      - 1.1.1.1
    volumes:
      - ./:/var/www
    networks:
      - scanyuk-network

  web:
    image: nginx:alpine
    container_name: scanyuk-web
    restart: unless-stopped
    ports:
      - "8081:80"
    volumes:
      - ./:/var/www
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    networks:
      - scanyuk-network

  db:
    image: postgres:15-alpine
    container_name: scanyuk-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_DATABASE}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5433:5432"
    volumes:
      - scanyuk-db-data:/var/lib/postgresql/data
    networks:
      - scanyuk-network

  minio:
    image: minio/minio:latest
    container_name: scanyuk-minio
    environment:
      - MINIO_ROOT_USER=adminscanyuk
      - MINIO_ROOT_PASSWORD=RahasiaMinio123!
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - ./minio_data:/data
    restart: unless-stopped
    networks:
      - scanyuk-network

networks:
  scanyuk-network:
    driver: bridge

volumes:
  scanyuk-db-data: